<h1>Add a review</h1>

<div class="devise-input-form">
  <%= simple_form_for([@boat, @review]) do |f| %>
    <%= f.input :title%>
    <%= f.input :content %>
    <%= f.input :rating %>
    <%= f.submit %>
  <% end %>
</div>
