<%= render "shared/banner" %>

<h2>List of Boats</h2>
<div class="container">
 <div class="row">
   <% @boats.each do |b| %>
     <div class="col-xs-12 col-md-4">
       <%= render "shared/boatcard", name: b.name, review: b.reviews, link: boat_path(b) %>
     </div>
   <% end %>
 </div>
</div>
